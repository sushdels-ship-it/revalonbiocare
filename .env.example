# Email SMTP Configuration
# Copy this file to .env and fill in your values

# Gmail Configuration (recommended for development)
SMTP_USERNAME=revelonbiocare@gmail.com
SMTP_PASSWORD=icyvgvwbitxonpng
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Optional: Recipient email (defaults to SMTP_USERNAME if not set)
TO_EMAIL=revelonbiocare@gmail.com

# Server Port
PORT=3000

# Notes:
# 1. Gmail requires an App Password (NOT your regular password)
#    - Go to https://myaccount.google.com/security
#    - Enable 2-Step Verification (if not already enabled)
#    - Create App Password: Select "Mail" and "Windows Computer"
#    - Copy the 16-character password and paste above as SMTP_PASSWORD
#
# 2. For other email services, use their SMTP settings:
#    - SendGrid: host=smtp.sendgrid.net, port=587, user=apikey, pass=SG.xxxxx
#    - AWS SES: host=email-smtp.region.amazonaws.com, port=587, user/pass from IAM
#    - Ethereal (test): Use test account credentials (no real emails sent)
#
# 3. NEVER commit .env to git. It's in .gitignore for security.
